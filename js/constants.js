'use strict';

(function () {
  var OFFERS_NUM = 8;

  var PinSize = {
    WIDTH: 50,
    HEIGHT: 70,
    RADIUS: 25,
  };

  var MainPinSize = {
    WIDTH: 65,
    HEIGHT: 65,
    HEIGHT_WITH_POINTER: 80,
    RADIUS: 33,
  };

  window.constants = {
    OFFERS_NUM: OFFERS_NUM,
    PinSize: PinSize,
    MainPinSize: MainPinSize,
  };
})();
